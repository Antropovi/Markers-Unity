using UnityEngine;
using UnityEditor;
using System.Collections;

public class CodeUtils : Editor {

	[MenuItem("Assets/Create code texture")]
	public static void CreateCodeTexture()
	{
	/*	uint number = 32u;
		Texture2D t = MELEncoder.CreateCodeTexture (number);
		System.IO.File.WriteAllBytes ("code_" + number.ToString () + ".png", t.EncodeToPNG ());

		Debug.Log (MELEncoder.debugString);
        */
	}


}
